# EXAMPLE Secret (Phase 11, M4) — DO NOT commit real values.
# In production, generate this from a secret manager (External Secrets Operator,
# Vault Agent, Sealed Secrets, or cloud KMS). Values here are placeholders and
# will (correctly) fail the app's production startup validation.
apiVersion: v1
kind: Secret
metadata:
  name: ai-credit-secrets
  namespace: ai-credit
  labels:
    app.kubernetes.io/part-of: ai-credit-platform
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_WITH_openssl_rand_hex_32"
  CONNECTOR_MASTER_KEY: "REPLACE_WITH_openssl_rand_hex_32"
  DATABASE_URL: "postgresql+psycopg://credit:REPLACE@postgres:5432/credit"
