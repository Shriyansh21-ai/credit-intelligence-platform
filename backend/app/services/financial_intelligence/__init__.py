"""Advanced Financial Intelligence Platform services (Track 3).

Additive quantitative layer on top of Phases 1-11 / Tracks 1-2. Foundation:
``common`` (pure financial math) and ``data_access`` (read-only platform reads).
One module per milestone:

    treasury(M1) portfolio(M2) regulatory(M3) economic(M4) esg(M5) market(M6)
    altdata(M7) forecasting(M8) quant(M9) benchmarking(M10) executive(M11)
    optimization(M12) digital_twin(M13) strategic(M14)
"""
