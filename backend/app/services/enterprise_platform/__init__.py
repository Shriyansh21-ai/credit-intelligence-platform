"""Enterprise Productization & Commercial Readiness services.

Additive productization layer on top of Phases 1-11 / Tracks 1-3. Foundation
``common`` (pure helpers) and ``data_access`` (read-only platform reads). One
module per milestone

    ux(M1) workspaces(M2) developer(M3) marketplace(M4) integration(M5)
    data_mgmt(M6) operations(M7) security_center(M8) customer_success(M9)
    deployment(M10) monitoring(M11) bi(M12) launch(M13)
"""
