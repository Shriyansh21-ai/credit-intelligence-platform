"""Enterprise Banking Operating System services.

Additive AI-native layer on top of Phases 1-9. Each engine is a small, focused
service module reusing the shared helpers in :mod:`common` and the
deterministic helpers where relevant. Nothing here mutates existing behaviour.
"""
